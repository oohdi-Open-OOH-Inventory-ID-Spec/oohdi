services:
  oohdi-website:
    build:
      context: ..
      dockerfile: website/Dockerfile
    container_name: oohdi-website
    ports:
      - "18083:8080"
    environment:
      OOHDI_HOST: "0.0.0.0"
      OOHDI_PORT: "8080"
    restart: unless-stopped
